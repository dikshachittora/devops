apiVersion: apps/v1
kind: Deployment
metadata:
  name: liferay-deployment
  namespace: exam
spec:
  replicas: 1
  selector:
    matchLabels:
      app: liferay
  template:
    metadata:
      labels:
        app: liferay
    spec:
      containers:
      - image: dikshaop/devops:devops-test-liferay7.4.3.112-ga112-1368
        name: liferay
        ports:
        - containerPort: 8009
        resources:
          limits:
            cpu: "2"
          requests:
            cpu: "2"
        volumeMounts:
        - mountPath: /opt/liferay/data
          name: liferay-data-volume
        - mountPath: /opt/liferay/tomcat/logs
          name: liferay-logs-volume
      imagePullSecrets:
      - name: docker-registry-secret
      nodeName: st04
      volumes:
      - name: liferay-data-volume
        persistentVolumeClaim:
          claimName: liferay-pvc-data
      - name: liferay-logs-volume
        persistentVolumeClaim:
          claimName: liferay-pvc-logs
